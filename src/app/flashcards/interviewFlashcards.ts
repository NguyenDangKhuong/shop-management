export type Topic = 'JavaScript' | 'React' | 'CSS' | 'HTML' | 'Web APIs' | 'Security' | 'Performance' | 'Testing' | 'Async' | 'OOP' | 'Networking' | 'Closure'
export type Difficulty = 'Easy' | 'Medium' | 'Hard'

export interface InterviewCard {
    id: string
    question: string
    answer_vi: string
    answer_en: string
    topic: Topic
    difficulty: Difficulty
}

export const interviewCards: InterviewCard[] = [
// ═══════════════════════════════════════
//  JAVASCRIPT
// ═══════════════════════════════════════
{id:'js-hoisting',question:'Explain the concept of "hoisting" in JavaScript',answer_vi:'Hoisting = JS "đẩy" khai báo biến và function lên đầu scope TRƯỚC khi code chạy. var được hoisted với giá trị undefined, let/const bị hoisted nhưng nằm trong Temporal Dead Zone (TDZ) → ReferenceError nếu truy cập trước khai báo. Function declarations được hoisted hoàn toàn (cả body), function expressions thì KHÔNG.',answer_en:'Hoisting = JS "moves" variable and function declarations to the top of their scope BEFORE code runs. var is hoisted with value undefined, let/const are hoisted but placed in Temporal Dead Zone (TDZ) → ReferenceError if accessed before declaration. Function declarations are fully hoisted (including body), function expressions are NOT.',topic:'JavaScript',difficulty:'Hard'},
{id:'js-var-let-const',question:'What are the differences between JavaScript variables created using `let`, `var` or `const`?',answer_vi:'var: function scope, hoisted = undefined, cho phép re-declare. let: block scope, TDZ, không re-declare, cho phép re-assign. const: block scope, TDZ, không re-declare, không re-assign (nhưng object/array bên trong vẫn mutable). → Luôn dùng const mặc định, let khi cần thay đổi, tránh var.',answer_en:'var: function scope, hoisted = undefined, allows re-declare. let: block scope, TDZ, no re-declare, allows re-assign. const: block scope, TDZ, no re-declare, no re-assign (but object/array contents are still mutable). → Default to const, use let when reassignment needed, avoid var.',topic:'JavaScript',difficulty:'Medium'},
{id:'js-equality',question:'What is the difference between `==` and `===` in JavaScript?',answer_vi:'== (loose equality) tự ép kiểu (type coercion) trước khi so sánh: "1" == 1 → true. === (strict equality) so sánh CẢ type lẫn value: "1" === 1 → false. → Luôn dùng === để tránh bug khó hiểu. Ngoại lệ duy nhất: val == null kiểm tra cả null và undefined.',answer_en:'== (loose equality) performs type coercion before comparing: "1" == 1 → true. === (strict equality) compares BOTH type AND value: "1" === 1 → false. → Always use === to avoid subtle bugs. Only exception: val == null checks both null and undefined.',topic:'JavaScript',difficulty:'Easy'},
{id:'js-null-undefined',question:"What's the difference between a JavaScript variable that is: `null`, `undefined` or undeclared?",answer_vi:'undefined: biến đã khai báo nhưng chưa gán giá trị. null: giá trị được gán CỐ Ý để biểu thị "không có gì". undeclared: biến chưa được khai báo → ReferenceError. typeof undefined = "undefined", typeof null = "object" (bug lịch sử của JS).',answer_en:'undefined: variable declared but not assigned a value. null: value intentionally assigned to mean "nothing". undeclared: variable never declared → ReferenceError. typeof undefined = "undefined", typeof null = "object" (historical JS bug).',topic:'JavaScript',difficulty:'Easy'},
{id:'js-closure',question:'What is a closure in JavaScript, and how/why would you use one?',answer_vi:'Closure = function "nhớ" biến từ scope cha ngay cả sau khi scope cha đã kết thúc. Dùng để: 1) Tạo private variables 2) Data encapsulation 3) Function factories 4) Module pattern. Ví dụ: function counter() { let count = 0; return () => ++count } — count bị "đóng kín" bên trong.',answer_en:'Closure = function that "remembers" variables from its parent scope even after the parent has finished executing. Used for: 1) Private variables 2) Data encapsulation 3) Function factories 4) Module pattern. Example: function counter() { let count = 0; return () => ++count } — count is "enclosed" inside.',topic:'Closure',difficulty:'Medium'},
{id:'js-this',question:'Explain how `this` works in JavaScript',answer_vi:'this phụ thuộc vào CÁCH function được gọi: 1) Default: window/undefined 2) Implicit: obj.method() → this = obj 3) Explicit: call/apply/bind → this = arg đầu 4) new: this = object mới 5) Arrow: KHÔNG có this riêng, kế thừa từ scope cha. Thứ tự ưu tiên: new > explicit > implicit > default.',answer_en:'this depends on HOW the function is called: 1) Default: window/undefined 2) Implicit: obj.method() → this = obj 3) Explicit: call/apply/bind → this = first arg 4) new: this = new object 5) Arrow: NO own this, inherits from parent scope. Priority: new > explicit > implicit > default.',topic:'JavaScript',difficulty:'Hard'},
{id:'js-call-apply',question:"What's the difference between `.call` and `.apply` in JavaScript?",answer_vi:'Cả hai đều gọi function NGAY với this được chỉ định. Khác nhau ở cách truyền args: call truyền RIÊNG LẺ: fn.call(obj, a, b). apply truyền MẢNG: fn.apply(obj, [a, b]). Mẹo nhớ: Apply = Array, Call = Comma.',answer_en:'Both invoke the function IMMEDIATELY with specified this. Difference is how args are passed: call passes INDIVIDUALLY: fn.call(obj, a, b). apply passes as ARRAY: fn.apply(obj, [a, b]). Mnemonic: Apply = Array, Call = Comma.',topic:'JavaScript',difficulty:'Easy'},
{id:'js-bind',question:'Explain `Function.prototype.bind` in JavaScript',answer_vi:'bind() tạo function MỚI với this "khóa cứng" vĩnh viễn, KHÔNG gọi ngay. const bound = fn.bind(obj) → bound() luôn có this = obj. Dùng khi: truyền method làm callback (event handler), React class component this.handleClick.bind(this). Khác call/apply vì KHÔNG gọi ngay.',answer_en:'bind() creates a NEW function with this permanently "locked", does NOT invoke immediately. const bound = fn.bind(obj) → bound() always has this = obj. Used when: passing method as callback (event handler), React class components this.handleClick.bind(this). Unlike call/apply — does NOT invoke.',topic:'JavaScript',difficulty:'Medium'},
{id:'js-prototype',question:'Explain how prototypal inheritance works in JavaScript',answer_vi:'Mỗi object có link ẩn __proto__ trỏ đến prototype. Khi truy cập property không có → JS đi lên prototype chain tìm. Chain: obj → Object.prototype → null. ES6 class là syntactic sugar, bên dưới vẫn dùng prototype. Khác classical inheritance: object kế thừa trực tiếp từ object, không cần class blueprint.',answer_en:'Every object has a hidden __proto__ link to its prototype. When accessing a missing property → JS walks up the prototype chain to find it. Chain: obj → Object.prototype → null. ES6 classes are syntactic sugar, underneath still use prototypes. Unlike classical: objects inherit directly from objects, no class blueprint needed.',topic:'OOP',difficulty:'Medium'},
{id:'js-event-loop',question:'What is the event loop in JavaScript runtimes?',answer_vi:'JS single-threaded, Event Loop cho phép xử lý async. Cơ chế: 1) Call Stack chạy sync code 2) Web APIs xử lý async (setTimeout, fetch) 3) Khi xong → callback vào Queue 4) Event Loop: khi Stack RỖNG → lấy task từ Queue. Microtask (Promise) ưu tiên hơn Macrotask (setTimeout).',answer_en:'JS is single-threaded, Event Loop enables async handling. Mechanism: 1) Call Stack runs sync code 2) Web APIs handle async (setTimeout, fetch) 3) When done → callback goes to Queue 4) Event Loop: when Stack is EMPTY → takes task from Queue. Microtasks (Promise) have priority over Macrotasks (setTimeout).',topic:'JavaScript',difficulty:'Hard'},
{id:'js-event-delegation',question:'Explain event delegation in JavaScript',answer_vi:'Thay vì gắn listener cho TỪNG child element, gắn 1 listener cho parent và dùng event.target xác định child nào bị click. Lợi ích: 1) Tiết kiệm memory 2) Tự động xử lý dynamic elements 3) Ít code hơn. Hoạt động nhờ event bubbling — event đi từ child lên parent.',answer_en:'Instead of attaching a listener to EACH child element, attach ONE listener to the parent and use event.target to identify which child was clicked. Benefits: 1) Saves memory 2) Automatically handles dynamic elements 3) Less code. Works thanks to event bubbling — events travel from child up to parent.',topic:'JavaScript',difficulty:'Medium'},
{id:'js-event-bubbling',question:'Describe event bubbling in JavaScript and browsers',answer_vi:'Event bubbling: khi event xảy ra trên element, nó "nổi bọt" lên qua TẤT CẢ ancestors. Click button → button → div → body → html → document. Dùng event.stopPropagation() để dừng. Ngược lại là capturing (đi từ trên xuống). Thứ tự: Capturing → Target → Bubbling.',answer_en:'Event bubbling: when an event occurs on an element, it "bubbles up" through ALL ancestors. Click button → button → div → body → html → document. Use event.stopPropagation() to stop it. Opposite is capturing (top-down). Order: Capturing → Target → Bubbling.',topic:'JavaScript',difficulty:'Medium'},
{id:'js-event-capturing',question:'Describe event capturing in JavaScript and browsers',answer_vi:'Capturing là phase ĐẦU TIÊN: event đi TỪ TRÊN XUỐNG — document → html → body → target. Mặc định listener chạy ở bubbling phase. Để bắt ở capturing: addEventListener(event, handler, true). Ít dùng hơn bubbling, nhưng hữu ích khi cần xử lý event TRƯỚC KHI đến target.',answer_en:'Capturing is the FIRST phase: event travels TOP-DOWN — document → html → body → target. By default, listeners fire during bubbling phase. To catch during capturing: addEventListener(event, handler, true). Less common than bubbling, but useful when you need to handle events BEFORE they reach the target.',topic:'JavaScript',difficulty:'Medium'},
{id:'js-scope',question:'Explain the concept of scope in JavaScript',answer_vi:'Scope = phạm vi truy cập biến. 3 loại: 1) Global scope: truy cập ở mọi nơi 2) Function scope: biến var chỉ sống trong function 3) Block scope: biến let/const chỉ sống trong {}. Scope chain: code tìm biến từ scope hiện tại → lên scope cha → lên global.',answer_en:'Scope = where variables are accessible. 3 types: 1) Global scope: accessible everywhere 2) Function scope: var lives only inside function 3) Block scope: let/const lives only inside {}. Scope chain: code looks for variables from current scope → parent scope → global.',topic:'JavaScript',difficulty:'Easy'},
{id:'js-lexical-scope',question:'Explain the concept of lexical scoping',answer_vi:'Lexical scope = scope được xác định bởi VỊ TRÍ VIẾT code, không phải nơi gọi. Function "nhớ" scope nơi nó được ĐỊNH NGHĨA. Đây là cơ sở của closure. Arrow function dùng lexical this — this được xác định bởi nơi viết, không phải nơi gọi.',answer_en:'Lexical scope = scope determined by WHERE the code is WRITTEN, not where it\'s called. Functions "remember" the scope where they were DEFINED. This is the basis of closures. Arrow functions use lexical this — this is determined by where it\'s written, not where it\'s called.',topic:'JavaScript',difficulty:'Easy'},
{id:'js-higher-order',question:'What is the definition of a higher-order function in JavaScript?',answer_vi:'Higher-order function = function nhận function khác làm tham số HOẶC return function. Ví dụ built-in: map, filter, reduce, forEach, sort. Ví dụ custom: function multiply(factor) { return x => x * factor }. HOF là nền tảng của Functional Programming.',answer_en:'Higher-order function = function that takes another function as argument OR returns a function. Built-in examples: map, filter, reduce, forEach, sort. Custom example: function multiply(factor) { return x => x * factor }. HOFs are the foundation of Functional Programming.',topic:'JavaScript',difficulty:'Medium'},
{id:'js-arrow-syntax',question:'What advantage is there for using the JavaScript arrow syntax for a method in a constructor?',answer_vi:'Arrow function trong constructor/class giữ this = instance bất kể cách gọi. Không cần .bind(this) trong constructor. Lý do: arrow KHÔNG có this riêng → kế thừa this từ constructor scope. Trade-off: mỗi instance tạo bản copy mới (không share qua prototype).',answer_en:'Arrow function in constructor/class keeps this = instance regardless of how it\'s called. No need for .bind(this) in constructor. Reason: arrow has NO own this → inherits this from constructor scope. Trade-off: each instance creates a new copy (not shared via prototype).',topic:'JavaScript',difficulty:'Medium'},
{id:'js-anonymous',question:"What's a typical use case for anonymous functions in JavaScript?",answer_vi:'1) Callback: setTimeout(() => {}, 100) 2) IIFE: (function() {})() 3) Event handler: btn.addEventListener("click", () => {}) 4) Array methods: arr.map(x => x*2) 5) Promise: .then(data => {}). Ưu điểm: ngắn gọn, không "ô nhiễm" namespace. Nhược: khó debug (stack trace không có tên).',answer_en:'1) Callbacks: setTimeout(() => {}, 100) 2) IIFE: (function() {})() 3) Event handlers: btn.addEventListener("click", () => {}) 4) Array methods: arr.map(x => x*2) 5) Promises: .then(data => {}). Pros: concise, no namespace pollution. Cons: harder to debug (no name in stack trace).',topic:'JavaScript',difficulty:'Medium'},
{id:'js-create-objects',question:'What are the various ways to create objects in JavaScript?',answer_vi:'1) Object literal: {} 2) new Object() 3) Constructor: new Person() 4) Object.create(proto) — chỉ định prototype 5) ES6 Class 6) Factory function: return {} 7) Spread: {...obj}. Phổ biến nhất: literal {} và class. Object.create dùng khi cần kiểm soát prototype chain.',answer_en:'1) Object literal: {} 2) new Object() 3) Constructor: new Person() 4) Object.create(proto) — sets prototype 5) ES6 Class 6) Factory function: return {} 7) Spread: {...obj}. Most common: literal {} and class. Object.create when you need prototype chain control.',topic:'JavaScript',difficulty:'Medium'},
{id:'js-iife',question:"Explain why the following doesn't work as an IIFE: `function foo(){}();`. What needs to be changed?",answer_vi:'JS parser đọc function foo(){} như declaration (statement), không phải expression → không thể gọi ngay. Fix: bọc trong (): (function foo(){})() hoặc dùng unary operator: !function(){}() hoặc void function(){}(). IIFE cần function EXPRESSION, không phải declaration.',answer_en:'JS parser reads function foo(){} as a declaration (statement), not an expression → can\'t invoke immediately. Fix: wrap in (): (function foo(){})() or use unary operator: !function(){}() or void function(){}(). IIFE needs a function EXPRESSION, not a declaration.',topic:'JavaScript',difficulty:'Hard'},
{id:'js-use-strict',question:"What is `'use strict';` in JavaScript for?",answer_vi:'Strict mode bật kiểm tra nghiêm ngặt: 1) Cấm dùng biến chưa khai báo 2) Cấm delete biến 3) Cấm duplicate params 4) this = undefined thay vì window 5) Cấm with statement 6) Cấm octal syntax. ES6 modules tự động strict mode. Giúp catch lỗi sớm.',answer_en:'Strict mode enables strict checks: 1) Forbids undeclared variables 2) Forbids deleting variables 3) Forbids duplicate params 4) this = undefined instead of window 5) Forbids with statement 6) Forbids octal syntax. ES6 modules are automatically strict. Helps catch errors early.',topic:'JavaScript',difficulty:'Hard'},
{id:'js-async-sync',question:'Explain the difference between synchronous and asynchronous functions in JavaScript',answer_vi:'Sync: chạy tuần tự, block thread — dòng sau đợi dòng trước xong. Async: KHÔNG block — code tiếp tục chạy, kết quả trả về sau qua callback/Promise/async-await. JS single-threaded nhưng async nhờ Event Loop + Web APIs (setTimeout, fetch, I/O).',answer_en:'Sync: runs sequentially, blocks thread — next line waits for previous to finish. Async: does NOT block — code continues running, result returned later via callback/Promise/async-await. JS is single-threaded but handles async through Event Loop + Web APIs (setTimeout, fetch, I/O).',topic:'Async',difficulty:'Medium'},
{id:'js-promise-vs-callback',question:'What are the pros and cons of using Promises instead of callbacks in JavaScript?',answer_vi:'Pros: 1) Tránh callback hell (pyramid of doom) 2) .then() chain dễ đọc 3) .catch() xử lý lỗi tập trung 4) Promise.all/race cho parallel 5) Dùng với async/await. Cons: 1) Không thể cancel (nếu không dùng AbortController) 2) Phức tạp hơn callback đơn giản 3) Unhandled rejection cần xử lý.',answer_en:'Pros: 1) Avoids callback hell (pyramid of doom) 2) .then() chain is readable 3) .catch() centralizes error handling 4) Promise.all/race for parallel ops 5) Works with async/await. Cons: 1) Can\'t cancel (without AbortController) 2) More complex than simple callbacks 3) Unhandled rejections need handling.',topic:'Async',difficulty:'Medium'},
{id:'js-data-types',question:'What are the various data types in JavaScript?',answer_vi:'Primitives (7): string, number, bigint, boolean, undefined, null, symbol. Reference (1): object (bao gồm Array, Function, Date, RegExp, Map, Set). typeof null = "object" (bug). Primitives immutable + so sánh by value. Objects mutable + so sánh by reference.',answer_en:'Primitives (7): string, number, bigint, boolean, undefined, null, symbol. Reference (1): object (includes Array, Function, Date, RegExp, Map, Set). typeof null = "object" (bug). Primitives are immutable + compared by value. Objects are mutable + compared by reference.',topic:'JavaScript',difficulty:'Easy'},
{id:'js-spread-rest',question:'What are the benefits of using spread syntax in JavaScript and how is it different from rest syntax?',answer_vi:'Spread (...) TRẢI ra: [...arr], {...obj} — clone, merge, truyền args. Rest (...) GOM lại: function(...args) {} — gom params còn lại thành array. Cùng syntax nhưng NGƯỢC nhau: spread = unpack, rest = pack. Spread tạo shallow copy, không deep copy.',answer_en:'Spread (...) EXPANDS: [...arr], {...obj} — clone, merge, pass args. Rest (...) COLLECTS: function(...args) {} — gathers remaining params into array. Same syntax but OPPOSITE purpose: spread = unpack, rest = pack. Spread creates shallow copy, not deep copy.',topic:'JavaScript',difficulty:'Medium'},
{id:'js-mutable-immutable',question:'Explain the difference between mutable and immutable objects in JavaScript',answer_vi:'Mutable: thay đổi được sau khi tạo (object, array). Immutable: KHÔNG thay đổi được (primitives: string, number). Immutability trong FP: KHÔNG mutate state, tạo bản mới: {...obj, key: newVal}, [...arr, newItem]. Object.freeze() chỉ freeze level 1 (shallow). Immutability giúp predictable, dễ debug.',answer_en:'Mutable: can be changed after creation (object, array). Immutable: CANNOT be changed (primitives: string, number). Immutability in FP: DON\'T mutate state, create new copies: {...obj, key: newVal}, [...arr, newItem]. Object.freeze() only freezes level 1 (shallow). Immutability = predictable, easier to debug.',topic:'JavaScript',difficulty:'Medium'},
{id:'js-map-vs-object',question:'What is the difference between a `Map` object and a plain object in JavaScript?',answer_vi:'Map: key = BẤT KỲ type (object, function, number). Giữ thứ tự insert. .size property. Iterable trực tiếp. Tốt cho dynamic keys. Object: key chỉ là string/symbol. Không đảm bảo thứ tự (trước ES2015). Có prototype chain. Tốt cho fixed structure, JSON.',answer_en:'Map: key = ANY type (object, function, number). Preserves insertion order. .size property. Directly iterable. Good for dynamic keys. Object: keys are only string/symbol. Order not guaranteed (pre-ES2015). Has prototype chain. Good for fixed structure, JSON.',topic:'JavaScript',difficulty:'Easy'},
{id:'js-weakmap-weakset',question:'What are the differences between `Map`/`Set` and `WeakMap`/`WeakSet` in JavaScript?',answer_vi:'WeakMap/WeakSet: key phải là OBJECT, giữ weak reference → GC có thể thu hồi key bất cứ lúc nào. Không iterable, không có .size. Dùng cho: cache, private data, DOM node tracking — tự cleanup khi object bị GC. Map/Set: key = any, strong reference, iterable, có .size.',answer_en:'WeakMap/WeakSet: keys must be OBJECTS, hold weak references → GC can reclaim keys anytime. Not iterable, no .size. Used for: caching, private data, DOM node tracking — auto-cleanup when object is GC\'d. Map/Set: key = any, strong reference, iterable, has .size.',topic:'JavaScript',difficulty:'Medium'},
{id:'js-symbol',question:'What are `Symbol`s used for in JavaScript?',answer_vi:'Symbol = primitive type tạo giá trị unique, không trùng lặp. Dùng cho: 1) Property keys private/unique (tránh name collision) 2) Well-known Symbols: Symbol.iterator, Symbol.toPrimitive 3) Enum-like constants. typeof Symbol() = "symbol". Symbol("desc") !== Symbol("desc").',answer_en:'Symbol = primitive type that creates unique, non-duplicate values. Used for: 1) Private/unique property keys (avoid name collisions) 2) Well-known Symbols: Symbol.iterator, Symbol.toPrimitive 3) Enum-like constants. typeof Symbol() = "symbol". Symbol("desc") !== Symbol("desc").',topic:'JavaScript',difficulty:'Medium'},
{id:'js-es6-class-vs-es5',question:'What are the differences between JavaScript ES2015 classes and ES5 function constructors?',answer_vi:'ES6 class = syntactic sugar over prototypes. Khác biệt: 1) class syntax sạch hơn 2) extends + super dễ hơn prototype chain thủ công 3) class KHÔNG hoisted 4) class body luôn strict mode 5) static methods dễ định nghĩa 6) private fields (#field). Bên dưới vẫn là prototype.',answer_en:'ES6 class = syntactic sugar over prototypes. Differences: 1) class syntax is cleaner 2) extends + super easier than manual prototype chain 3) class is NOT hoisted 4) class body is always strict mode 5) static methods easy to define 6) private fields (#field). Underneath still prototypes.',topic:'OOP',difficulty:'Hard'},
{id:'js-commonjs-esm',question:'Explain the differences between CommonJS modules and ES modules in JavaScript',answer_vi:'CommonJS: require()/module.exports, sync, Node.js, runtime loading, dynamic paths. ESM: import/export, async, browser + Node, static analysis → tree shaking, top-level await. ESM là tiêu chuẩn tương lai. CommonJS vẫn phổ biến trong Node.js ecosystem.',answer_en:'CommonJS: require()/module.exports, sync, Node.js, runtime loading, dynamic paths. ESM: import/export, async, browser + Node, static analysis → tree shaking, top-level await. ESM is the future standard. CommonJS still common in Node.js ecosystem.',topic:'JavaScript',difficulty:'Hard'},
{id:'js-gc',question:'How does JavaScript garbage collection work?',answer_vi:'JS dùng Mark-and-Sweep: 1) GC đánh dấu tất cả objects reachable từ roots (global, stack, closures) 2) Objects không được đánh dấu = unreachable → bị thu hồi. Không cần quản lý memory thủ công. Memory leak xảy ra khi giữ reference không cần thiết (global vars, closures, event listeners, timers).',answer_en:'JS uses Mark-and-Sweep: 1) GC marks all objects reachable from roots (global, stack, closures) 2) Unmarked objects = unreachable → collected. No manual memory management needed. Memory leaks occur when holding unnecessary references (global vars, closures, event listeners, timers).',topic:'JavaScript',difficulty:'Hard'},
{id:'js-polyfill',question:'What are JavaScript polyfills for?',answer_vi:'Polyfill = code bắt chước API mới cho browser cũ không hỗ trợ. Ví dụ: Array.prototype.includes polyfill cho IE11. Kiểm tra feature trước: if (!Array.prototype.includes) { ... }. core-js là thư viện polyfill phổ biến nhất. Khác transpiling (Babel) — polyfill thêm runtime, transpile đổi syntax.',answer_en:'Polyfill = code that replicates a new API for older browsers that don\'t support it. Example: Array.prototype.includes polyfill for IE11. Check feature first: if (!Array.prototype.includes) { ... }. core-js is the most popular polyfill library. Different from transpiling (Babel) — polyfill adds runtime, transpile changes syntax.',topic:'JavaScript',difficulty:'Medium'},
{id:'js-debounce-throttle',question:'Explain the concept of debouncing and throttling',answer_vi:'Debounce: đợi user NGỪNG action X ms rồi mới chạy (ví dụ: search input — chỉ gọi API sau khi user ngừng gõ 300ms). Throttle: chạy TỐI ĐA 1 lần mỗi X ms (ví dụ: scroll handler — chỉ fire 1 lần/100ms dù scroll liên tục). Debounce = "đợi xong hãy làm", Throttle = "làm đều đặn".',answer_en:'Debounce: wait until user STOPS action for X ms then execute (e.g., search input — only call API after user stops typing for 300ms). Throttle: execute at MOST once per X ms (e.g., scroll handler — fire once/100ms even if scrolling continuously). Debounce = "wait till done", Throttle = "do it steadily".',topic:'Performance',difficulty:'Easy'},
{id:'js-shallow-deep-copy',question:'Explain the difference between shallow copy and deep copy',answer_vi:'Shallow copy: copy level 1, nested objects vẫn share reference. Cách: {...obj}, [...arr], Object.assign. Deep copy: copy TẤT CẢ levels, hoàn toàn independent. Cách: structuredClone(obj), JSON.parse(JSON.stringify(obj)) (không hỗ trợ function, Date, undefined).',answer_en:'Shallow copy: copies level 1, nested objects still share reference. Methods: {...obj}, [...arr], Object.assign. Deep copy: copies ALL levels, completely independent. Methods: structuredClone(obj), JSON.parse(JSON.stringify(obj)) (doesn\'t support function, Date, undefined).',topic:'JavaScript',difficulty:'Easy'},
{id:'js-currying',question:'What is currying and how does it work?',answer_vi:'Currying = chuyển function nhiều args thành chuỗi functions MỖI function nhận 1 arg: f(a,b,c) → f(a)(b)(c). Ví dụ: const add = a => b => a + b; add(2)(3) = 5. Dùng cho: partial application, function composition, reusable utilities. Kết hợp closure để "nhớ" args trước đó.',answer_en:'Currying = transform multi-arg function into a chain of functions EACH taking 1 arg: f(a,b,c) → f(a)(b)(c). Example: const add = a => b => a + b; add(2)(3) = 5. Used for: partial application, function composition, reusable utilities. Uses closures to "remember" previous args.',topic:'Closure',difficulty:'Easy'},
{id:'js-promise-states',question:'Explain the different states of a Promise',answer_vi:'3 trạng thái: 1) Pending: đang chờ, chưa có kết quả 2) Fulfilled: thành công, có value → .then() 3) Rejected: thất bại, có reason → .catch(). Chuyển trạng thái CHỈ 1 LẦN: pending → fulfilled HOẶC pending → rejected. Không thể đổi ngược.',answer_en:'3 states: 1) Pending: in progress, no result yet 2) Fulfilled: succeeded, has value → .then() 3) Rejected: failed, has reason → .catch(). State changes ONLY ONCE: pending → fulfilled OR pending → rejected. Cannot reverse.',topic:'Async',difficulty:'Easy'},
{id:'js-promise-all',question:'What is the use of `Promise.all()`',answer_vi:'Promise.all([p1, p2, p3]): chạy song song, đợi TẤT CẢ xong → trả array kết quả. Nếu BẤT KỲ promise nào reject → toàn bộ reject. Promise.allSettled: đợi tất cả kể cả reject. Promise.race: trả về promise ĐẦU TIÊN settle. Promise.any: trả về promise đầu tiên FULFILLED.',answer_en:'Promise.all([p1, p2, p3]): runs in parallel, waits for ALL to complete → returns array of results. If ANY promise rejects → whole thing rejects. Promise.allSettled: waits for all including rejections. Promise.race: returns FIRST settled promise. Promise.any: returns first FULFILLED promise.',topic:'Async',difficulty:'Easy'},
{id:'js-iterators-generators',question:'What are iterators and generators in JavaScript and what are they used for?',answer_vi:'Iterator: object có .next() trả {value, done}. Cho phép duyệt custom. Generator: function* + yield — tạm dừng và resume, tạo iterator dễ dàng. Dùng cho: lazy evaluation, infinite sequences, async flow control (Redux-Saga). for...of dùng iterator protocol.',answer_en:'Iterator: object with .next() returning {value, done}. Enables custom traversal. Generator: function* + yield — pause and resume, creates iterators easily. Used for: lazy evaluation, infinite sequences, async flow control (Redux-Saga). for...of uses the iterator protocol.',topic:'JavaScript',difficulty:'Hard'},
{id:'js-proxy',question:'What are proxies in JavaScript used for?',answer_vi:'Proxy = "bọc" object để intercept/customize operations: get, set, has, deleteProperty, apply. Dùng cho: 1) Validation 2) Logging/debugging 3) Default values 4) Data binding (Vue 3 reactivity) 5) API mocking. const proxy = new Proxy(target, handler). Reflect API dùng cùng Proxy.',answer_en:'Proxy = "wraps" an object to intercept/customize operations: get, set, has, deleteProperty, apply. Used for: 1) Validation 2) Logging/debugging 3) Default values 4) Data binding (Vue 3 reactivity) 5) API mocking. const proxy = new Proxy(target, handler). Reflect API used with Proxy.',topic:'JavaScript',difficulty:'Easy'},
{id:'js-workers',question:'What are workers in JavaScript used for?',answer_vi:'Web Workers chạy JS trên BACKGROUND THREAD, không block main thread (UI). Giao tiếp qua postMessage/onmessage. Dùng cho: heavy computation, data processing, image manipulation. Worker KHÔNG truy cập DOM, window, document. SharedWorker chia sẻ giữa tabs. Service Worker cho offline/cache.',answer_en:'Web Workers run JS on BACKGROUND THREAD, don\'t block main thread (UI). Communicate via postMessage/onmessage. Used for: heavy computation, data processing, image manipulation. Workers CANNOT access DOM, window, document. SharedWorker shares between tabs. Service Worker for offline/cache.',topic:'JavaScript',difficulty:'Medium'},
{id:'js-storage',question:'Describe the difference between a cookie, `sessionStorage` and `localStorage` in browsers',answer_vi:'Cookie: 4KB, gửi lên server mỗi request, có expiry, httpOnly/secure. localStorage: 5-10MB, persist vĩnh viễn, chỉ client-side. sessionStorage: 5-10MB, mất khi đóng tab. Dùng cookie cho auth tokens. localStorage cho preferences. sessionStorage cho form data tạm.',answer_en:'Cookie: 4KB, sent to server with every request, has expiry, httpOnly/secure. localStorage: 5-10MB, persists forever, client-side only. sessionStorage: 5-10MB, cleared when tab closes. Use cookies for auth tokens. localStorage for preferences. sessionStorage for temporary form data.',topic:'JavaScript',difficulty:'Medium'},
{id:'js-script-async-defer',question:'Describe the difference between `<script>`, `<script async>` and `<script defer>`',answer_vi:'<script>: block parsing, download + execute ngay. <script async>: download song song, execute NGAY khi download xong (block parsing lúc execute). <script defer>: download song song, execute SAU KHI HTML parse xong, theo thứ tự. → defer cho app scripts, async cho analytics/ads.',answer_en:'<script>: blocks parsing, downloads + executes immediately. <script async>: downloads in parallel, executes AS SOON as downloaded (blocks parsing during execution). <script defer>: downloads in parallel, executes AFTER HTML parsing complete, in order. → defer for app scripts, async for analytics/ads.',topic:'HTML',difficulty:'Medium'},
{id:'js-ajax',question:'Explain AJAX in as much detail as possible',answer_vi:'AJAX = Asynchronous JavaScript And XML. Gửi HTTP request mà KHÔNG reload trang. Flow: 1) JS tạo request (fetch/XMLHttpRequest) 2) Browser gửi async 3) Server xử lý, trả response 4) JS nhận và update DOM. Ngày nay dùng fetch() + JSON thay vì XMLHttpRequest + XML.',answer_en:'AJAX = Asynchronous JavaScript And XML. Sends HTTP requests WITHOUT page reload. Flow: 1) JS creates request (fetch/XMLHttpRequest) 2) Browser sends async 3) Server processes, returns response 4) JS receives and updates DOM. Today uses fetch() + JSON instead of XMLHttpRequest + XML.',topic:'Networking',difficulty:'Hard'},
{id:'js-xhr-fetch',question:'What are the differences between `XMLHttpRequest` and `fetch()` in JavaScript?',answer_vi:'fetch: Promise-based, cleaner syntax, streaming support, không gửi cookies mặc định, không reject on HTTP errors (404/500). XMLHttpRequest: callback-based, upload progress events, có thể cancel dễ hơn. fetch hiện đại hơn nhưng cần check response.ok thủ công.',answer_en:'fetch: Promise-based, cleaner syntax, streaming support, no cookies by default, doesn\'t reject on HTTP errors (404/500). XMLHttpRequest: callback-based, upload progress events, easier to cancel. fetch is more modern but needs manual response.ok check.',topic:'Networking',difficulty:'Hard'},
{id:'js-abort-controller',question:'How do you abort a web request using `AbortController` in JavaScript?',answer_vi:'const controller = new AbortController(); fetch(url, { signal: controller.signal }); controller.abort() — hủy request. Signal throws AbortError. Dùng cho: cancel search khi user gõ tiếp, cleanup trong useEffect, timeout requests. Có thể dùng 1 signal cho nhiều fetches.',answer_en:'const controller = new AbortController(); fetch(url, { signal: controller.signal }); controller.abort() — cancels request. Signal throws AbortError. Used for: cancel search on new input, cleanup in useEffect, timeout requests. One signal can cancel multiple fetches.',topic:'Networking',difficulty:'Hard'},
{id:'js-same-origin',question:'Explain the same-origin policy with regards to JavaScript',answer_vi:'Same-origin policy: browser chặn request đến domain/port/protocol KHÁC. origin = protocol + domain + port. Ví dụ: http://a.com không thể fetch https://b.com. Bypass: CORS headers (server), proxy, JSONP (cũ). Bảo vệ khỏi data theft giữa các sites.',answer_en:'Same-origin policy: browser blocks requests to DIFFERENT domain/port/protocol. origin = protocol + domain + port. Example: http://a.com can\'t fetch https://b.com. Bypass: CORS headers (server), proxy, JSONP (legacy). Protects against cross-site data theft.',topic:'Security',difficulty:'Hard'},
{id:'js-recursion',question:'What is recursion and how is it used in JavaScript?',answer_vi:'Recursion = function GỌI CHÍNH NÓ. Cần: 1) Base case (điều kiện dừng) 2) Recursive case (gọi lại với input nhỏ hơn). Dùng cho: tree traversal, nested structures (DOM), factorial, fibonacci. Cẩn thận: stack overflow nếu quên base case hoặc input quá lớn.',answer_en:'Recursion = function that CALLS ITSELF. Needs: 1) Base case (stopping condition) 2) Recursive case (call again with smaller input). Used for: tree traversal, nested structures (DOM), factorial, fibonacci. Careful: stack overflow if missing base case or input too large.',topic:'JavaScript',difficulty:'Easy'},
{id:'js-tree-shaking',question:'Explain the concept of tree shaking in module bundling',answer_vi:'Tree shaking = loại bỏ code KHÔNG DÙNG (dead code) khỏi bundle khi build. Hoạt động với ESM (import/export) vì static analysis. Ví dụ: import { map } from lodash-es → chỉ include map, bỏ hết phần còn lại. KHÔNG hoạt động với CommonJS require().',answer_en:'Tree shaking = removes UNUSED code (dead code) from bundle during build. Works with ESM (import/export) due to static analysis. Example: import { map } from lodash-es → only includes map, drops everything else. Does NOT work with CommonJS require().',topic:'Performance',difficulty:'Easy'},
{id:'js-object-freeze',question:'What is `Object.freeze()` for?',answer_vi:'Object.freeze(obj): KHÔNG thể thêm/xóa/sửa properties. Shallow freeze — nested objects vẫn mutable. Object.seal: không thêm/xóa nhưng CÓ THỂ sửa. Object.preventExtensions: không thêm nhưng có thể sửa/xóa. Dùng cho: constants, config không đổi.',answer_en:'Object.freeze(obj): CANNOT add/delete/modify properties. Shallow freeze — nested objects are still mutable. Object.seal: no add/delete but CAN modify. Object.preventExtensions: no add but can modify/delete. Used for: constants, immutable configs.',topic:'JavaScript',difficulty:'Easy'},
{id:'js-async-await',question:'What is async/await and how does it simplify asynchronous code?',answer_vi:'async function tự return Promise. await tạm dừng execution chờ Promise resolve. Code trông giống sync nhưng chạy async. try/catch xử lý lỗi thay vì .catch(). Dưới hood vẫn là Promises. Không dùng await ngoài async function (trừ top-level await trong ESM).',answer_en:'async function automatically returns a Promise. await pauses execution waiting for Promise to resolve. Code looks synchronous but runs asynchronously. try/catch handles errors instead of .catch(). Under the hood still Promises. Can\'t use await outside async function (except top-level await in ESM).',topic:'Async',difficulty:'Easy'},
{id:'js-xss',question:'What is Cross-Site Scripting (XSS) and how can you prevent it?',answer_vi:'XSS = attacker inject mã JS độc hại vào trang web. 3 loại: Stored (DB), Reflected (URL), DOM-based. Phòng: 1) Escape/sanitize user input 2) Content-Security-Policy header 3) httpOnly cookies 4) innerHTML → textContent 5) React tự escape JSX mặc định.',answer_en:'XSS = attacker injects malicious JS code into web page. 3 types: Stored (DB), Reflected (URL), DOM-based. Prevent: 1) Escape/sanitize user input 2) Content-Security-Policy header 3) httpOnly cookies 4) innerHTML → textContent 5) React auto-escapes JSX by default.',topic:'Security',difficulty:'Easy'},
{id:'js-csrf',question:'Explain the concept of Cross-Site Request Forgery (CSRF) and its mitigation techniques',answer_vi:'CSRF = attacker lừa user gửi request không mong muốn đến site khác (user đã đăng nhập). Phòng: 1) CSRF tokens 2) SameSite cookie 3) Check Origin/Referer header 4) Double-submit cookie. Ví dụ: trang evil gửi POST đến bank.com dùng cookie của user.',answer_en:'CSRF = attacker tricks user into sending unwanted request to another site (where user is logged in). Prevent: 1) CSRF tokens 2) SameSite cookie 3) Check Origin/Referer header 4) Double-submit cookie. Example: evil page sends POST to bank.com using user\'s cookies.',topic:'Security',difficulty:'Easy'},
{id:'js-csp',question:'Explain the concept of Content Security Policy (CSP) and how it enhances security',answer_vi:'CSP = HTTP header kiểm soát nguồn nào được phép load scripts/styles/images. Ví dụ: Content-Security-Policy: script-src \'self\' → chỉ cho phép scripts từ cùng domain. Chặn inline scripts, eval(). Giúp ngăn XSS. report-uri để log violations.',answer_en:'CSP = HTTP header controlling which sources can load scripts/styles/images. Example: Content-Security-Policy: script-src \'self\' → only allows scripts from same domain. Blocks inline scripts, eval(). Helps prevent XSS. report-uri to log violations.',topic:'Security',difficulty:'Easy'},
{id:'js-lazy-loading',question:'Explain the concept of lazy loading and how it can improve performance',answer_vi:'Lazy loading = chỉ load resources KHI CẦN, không load tất cả lúc đầu. Ví dụ: images dưới fold load khi scroll đến (loading="lazy"), code splitting (dynamic import), React.lazy() cho components. Giảm initial load time, tiết kiệm bandwidth.',answer_en:'Lazy loading = only load resources WHEN NEEDED, not everything upfront. Examples: below-fold images load on scroll (loading="lazy"), code splitting (dynamic import), React.lazy() for components. Reduces initial load time, saves bandwidth.',topic:'Performance',difficulty:'Easy'},
{id:'js-dom',question:'What is the DOM and how is it structured?',answer_vi:'DOM = Document Object Model — biểu diễn HTML dưới dạng tree objects. Browser parse HTML → tạo DOM tree. Mỗi element = node. document là root. Thao tác DOM: querySelector, createElement, appendChild, removeChild. DOM manipulation tốn performance → batch updates, virtual DOM (React).',answer_en:'DOM = Document Object Model — represents HTML as a tree of objects. Browser parses HTML → creates DOM tree. Each element = node. document is root. DOM manipulation: querySelector, createElement, appendChild, removeChild. DOM ops are expensive → batch updates, virtual DOM (React).',topic:'JavaScript',difficulty:'Easy'},
// ═══════════════════════════════════════
//  REACT
// ═══════════════════════════════════════
{id:'react-what',question:'What is React? Describe the benefits of React',answer_vi:'React = UI library (không phải framework) tạo UI bằng components. Lợi ích: 1) Component-based — reusable 2) Virtual DOM — update hiệu quả 3) One-way data flow — dễ debug 4) Huge ecosystem 5) JSX — viết UI trong JS 6) React Native cho mobile.',answer_en:'React = UI library (not framework) for building UI with components. Benefits: 1) Component-based — reusable 2) Virtual DOM — efficient updates 3) One-way data flow — easier debugging 4) Huge ecosystem 5) JSX — write UI in JS 6) React Native for mobile.',topic:'React',difficulty:'Medium'},
{id:'react-jsx',question:'What is JSX and how does it work?',answer_vi:'JSX = JavaScript XML — syntax mở rộng cho phép viết HTML-like trong JS. Babel compile JSX → React.createElement() calls. <div className="x"> → React.createElement("div", {className: "x"}). Không bắt buộc nhưng dễ đọc hơn. className thay class, htmlFor thay for.',answer_en:'JSX = JavaScript XML — syntax extension allowing HTML-like code in JS. Babel compiles JSX → React.createElement() calls. <div className="x"> → React.createElement("div", {className: "x"}). Not required but more readable. className instead of class, htmlFor instead of for.',topic:'React',difficulty:'Medium'},
{id:'react-node-element-component',question:'What is the difference between React Node, React Element, and a React Component?',answer_vi:'React Node: bất kỳ gì React render được (string, number, element, array, null). React Element: object mô tả UI — { type, props, key }. Immutable, lightweight. React Component: function/class RETURN elements. Component = blueprint, Element = instance, Node = renderable value.',answer_en:'React Node: anything React can render (string, number, element, array, null). React Element: object describing UI — { type, props, key }. Immutable, lightweight. React Component: function/class that RETURNS elements. Component = blueprint, Element = instance, Node = renderable value.',topic:'React',difficulty:'Medium'},
{id:'react-state-props',question:'What is the difference between state and props in React?',answer_vi:'Props: dữ liệu truyền TỪ CHA → CON, read-only, component con KHÔNG thể sửa. State: dữ liệu NỘI BỘ component, có thể thay đổi qua setState/useState, khi thay đổi → re-render. Props = input, State = memory. "Lifting state up" khi cần share state.',answer_en:'Props: data passed FROM PARENT → CHILD, read-only, child CANNOT modify. State: INTERNAL component data, changeable via setState/useState, changes trigger re-render. Props = input, State = memory. "Lift state up" when sharing state between siblings.',topic:'React',difficulty:'Easy'},
{id:'react-key',question:'What is the purpose of the `key` prop in React?',answer_vi:'key giúp React NHẬN DIỆN element nào thay đổi/thêm/xóa trong list. Phải unique trong cùng list. React dùng key để quyết định re-render hay reuse DOM. KHÔNG dùng index làm key khi list có thể thêm/xóa/sort — gây bug UI.',answer_en:'key helps React IDENTIFY which elements changed/added/removed in a list. Must be unique within the same list. React uses key to decide whether to re-render or reuse DOM. DON\'T use index as key when list can add/delete/sort — causes UI bugs.',topic:'React',difficulty:'Medium'},
{id:'react-key-index',question:'What is the consequence of using array indices as the value for `key`s in React?',answer_vi:'Index làm key gây: 1) Re-render SAI khi insert/delete/sort — React nghĩ element cũ vẫn ở vị trí đó 2) State bị "dính" vào index sai 3) Input values bị lẫn lộn. Chỉ OK khi: list static, không sort, không có unique id. Luôn ưu tiên unique id.',answer_en:'Index as key causes: 1) WRONG re-renders on insert/delete/sort — React thinks old element is still at that position 2) State gets "stuck" on wrong index 3) Input values get mixed up. Only OK when: list is static, no sorting, no unique id. Always prefer unique id.',topic:'React',difficulty:'Medium'},
{id:'react-controlled-uncontrolled',question:'What is the difference between controlled and uncontrolled React Components?',answer_vi:'Controlled: React state kiểm soát value — <input value={val} onChange={setVal}>. Form data trong React state. Uncontrolled: DOM tự quản lý value — dùng ref để đọc. Controlled: predictable, validate dễ. Uncontrolled: ít code, dùng cho file input, integration với non-React code.',answer_en:'Controlled: React state controls value — <input value={val} onChange={setVal}>. Form data lives in React state. Uncontrolled: DOM manages its own value — use ref to read. Controlled: predictable, easy validation. Uncontrolled: less code, used for file input, integration with non-React code.',topic:'React',difficulty:'Medium'},
{id:'react-hooks-benefits',question:'What are the benefits of using hooks in React?',answer_vi:'Hooks cho phép: 1) Dùng state/lifecycle trong function components 2) Logic reuse qua custom hooks (thay HOC/render props) 3) Code ngắn gọn hơn class 4) Tách logic theo concern, không theo lifecycle 5) Dễ test. useState, useEffect, useContext là 3 hooks cơ bản nhất.',answer_en:'Hooks allow: 1) State/lifecycle in function components 2) Logic reuse via custom hooks (replaces HOC/render props) 3) More concise than classes 4) Separate logic by concern, not by lifecycle 5) Easier to test. useState, useEffect, useContext are the 3 most essential hooks.',topic:'React',difficulty:'Medium'},
{id:'react-hooks-rules',question:'What are the rules of React hooks?',answer_vi:'2 rules: 1) CHỈ gọi hooks ở TOP LEVEL — không gọi trong if/for/nested functions 2) CHỈ gọi trong React function components hoặc custom hooks. Lý do: React dựa vào THỨ TỰ GỌI hooks để map state đúng. eslint-plugin-react-hooks kiểm tra tự động.',answer_en:'2 rules: 1) ONLY call hooks at TOP LEVEL — not inside if/for/nested functions 2) ONLY call in React function components or custom hooks. Reason: React relies on the ORDER of hook calls to map state correctly. eslint-plugin-react-hooks checks automatically.',topic:'React',difficulty:'Medium'},
{id:'react-useeffect-deps',question:'What does the dependency array of `useEffect` affect?',answer_vi:'[]: chạy 1 lần (mount). [a, b]: chạy khi a hoặc b thay đổi. Không truyền: chạy MỖI render. Return function = cleanup (chạy trước re-run hoặc unmount). Sai deps → stale closure hoặc infinite loop. ESLint exhaustive-deps rule giúp catch.',answer_en:'[]: runs once (mount). [a, b]: runs when a or b changes. No array: runs EVERY render. Return function = cleanup (runs before re-run or unmount). Wrong deps → stale closures or infinite loops. ESLint exhaustive-deps rule helps catch these.',topic:'React',difficulty:'Medium'},
{id:'react-useeffect-uselayouteffect',question:'What is the difference between `useEffect` and `useLayoutEffect` in React?',answer_vi:'useEffect: chạy SAU paint (async, non-blocking) — cho hầu hết side effects. useLayoutEffect: chạy SAU DOM update nhưng TRƯỚC paint (sync, blocking) — cho DOM measurements, animations, scroll position. useLayoutEffect hiếm khi cần, chỉ dùng khi thấy visual flicker.',answer_en:'useEffect: runs AFTER paint (async, non-blocking) — for most side effects. useLayoutEffect: runs AFTER DOM update but BEFORE paint (sync, blocking) — for DOM measurements, animations, scroll position. useLayoutEffect rarely needed, only use when you see visual flicker.',topic:'React',difficulty:'Medium'},
{id:'react-useref',question:'What is the `useRef` hook in React and when should it be used?',answer_vi:'useRef trả {current: value} — persist qua renders, thay đổi KHÔNG trigger re-render. Dùng cho: 1) Truy cập DOM element (ref.current = DOM node) 2) Lưu mutable value (timer id, prev state) 3) Focus input. Giống instance variable trong class.',answer_en:'useRef returns {current: value} — persists across renders, changes do NOT trigger re-render. Used for: 1) Access DOM elements (ref.current = DOM node) 2) Store mutable values (timer id, prev state) 3) Focus input. Like an instance variable in classes.',topic:'React',difficulty:'Medium'},
{id:'react-usecallback',question:'What is the `useCallback` hook in React and when should it be used?',answer_vi:'useCallback(fn, deps): memoize function reference — chỉ tạo function mới khi deps thay đổi. Dùng khi: 1) Truyền callback cho child component có React.memo 2) Dependency của useEffect. KHÔNG dùng everywhere — chỉ khi child re-render nhiều không cần thiết.',answer_en:'useCallback(fn, deps): memoizes function reference — only creates new function when deps change. Use when: 1) Passing callback to child with React.memo 2) Dependency of useEffect. DON\'T use everywhere — only when child re-renders unnecessarily.',topic:'React',difficulty:'Medium'},
{id:'react-usememo',question:'What is the `useMemo` hook in React and when should it be used?',answer_vi:'useMemo(() => compute, deps): memoize KẾT QUẢ tính toán — chỉ tính lại khi deps thay đổi. Dùng cho: expensive computations, derived data, referential equality cho objects/arrays. KHÔNG dùng cho tính toán nhẹ — overhead memoization có thể chậm hơn.',answer_en:'useMemo(() => compute, deps): memoizes COMPUTED result — only recalculates when deps change. Use for: expensive computations, derived data, referential equality for objects/arrays. DON\'T use for light computations — memoization overhead can be slower.',topic:'React',difficulty:'Medium'},
{id:'react-usereducer',question:'What is the `useReducer` hook in React and when should it be used?',answer_vi:'useReducer(reducer, initialState): quản lý complex state với pattern dispatch(action) → reducer tính state mới. Dùng khi: 1) State phức tạp (nhiều sub-values) 2) Next state phụ thuộc prev state 3) Logic state testable riêng. Giống Redux pattern.',answer_en:'useReducer(reducer, initialState): manages complex state with dispatch(action) → reducer computes new state. Use when: 1) Complex state (multiple sub-values) 2) Next state depends on prev state 3) State logic testable separately. Same pattern as Redux.',topic:'React',difficulty:'Medium'},
{id:'react-rerendering',question:'What does re-rendering mean in React?',answer_vi:'Re-render = React gọi lại component function để tạo virtual DOM mới → so sánh với cũ (reconciliation) → update DOM thực. Trigger: 1) setState 2) Parent re-render 3) Context change. Re-render ≠ DOM update — React chỉ update phần THAY ĐỔI.',answer_en:'Re-render = React calls component function again to create new virtual DOM → compares with old (reconciliation) → updates real DOM. Triggers: 1) setState 2) Parent re-render 3) Context change. Re-render ≠ DOM update — React only updates what CHANGED.',topic:'React',difficulty:'Hard'},
{id:'react-no-mutate-state',question:'Why does React recommend against mutating state?',answer_vi:'React dùng referential equality (===) để check state thay đổi. Mutate trực tiếp → reference KHÔNG đổi → React KHÔNG re-render. Phải tạo object/array MỚI: {...obj}, [...arr]. Immutability giúp: predictable, time-travel debugging, React.memo works, concurrent mode safe.',answer_en:'React uses referential equality (===) to check if state changed. Direct mutation → reference UNCHANGED → React does NOT re-render. Must create NEW object/array: {...obj}, [...arr]. Immutability enables: predictable, time-travel debugging, React.memo works, concurrent mode safe.',topic:'React',difficulty:'Hard'},
{id:'react-fragments',question:'What are React Fragments used for?',answer_vi:'Fragment <></> hoặc <Fragment> cho phép return nhiều elements KHÔNG cần wrapper div. Lợi ích: 1) Không thêm DOM node thừa 2) Giữ layout CSS sạch 3) <Fragment key={id}> khi cần key trong list. Cú pháp ngắn <></> không hỗ trợ key.',answer_en:'Fragment <></> or <Fragment> allows returning multiple elements WITHOUT a wrapper div. Benefits: 1) No extra DOM node 2) Keeps CSS layout clean 3) <Fragment key={id}> when key needed in lists. Short syntax <></> doesn\'t support key.',topic:'React',difficulty:'Medium'},
{id:'react-virtual-dom',question:'What is virtual DOM in React?',answer_vi:'Virtual DOM = bản copy nhẹ (JS object) của real DOM. Khi state thay đổi: 1) React tạo virtual DOM mới 2) Diff với virtual DOM cũ (reconciliation) 3) Chỉ update phần THAY ĐỔI trong real DOM (batch). Nhanh hơn thao tác real DOM trực tiếp vì DOM operations costly.',answer_en:'Virtual DOM = lightweight copy (JS object) of real DOM. On state change: 1) React creates new virtual DOM 2) Diffs with old virtual DOM (reconciliation) 3) Only updates CHANGED parts in real DOM (batch). Faster than direct real DOM manipulation since DOM operations are costly.',topic:'React',difficulty:'Hard'},
{id:'react-reconciliation',question:'What is reconciliation in React?',answer_vi:'Reconciliation = algorithm React dùng để diff 2 virtual DOM trees và tìm minimum DOM changes. 2 heuristics: 1) Khác type → destroy + rebuild subtree 2) Cùng type → update attributes, recurse children. key giúp React track list items hiệu quả.',answer_en:'Reconciliation = algorithm React uses to diff 2 virtual DOM trees and find minimum DOM changes. 2 heuristics: 1) Different type → destroy + rebuild subtree 2) Same type → update attributes, recurse children. key helps React track list items efficiently.',topic:'React',difficulty:'Hard'},
{id:'react-fiber',question:'What is React Fiber and how is it an improvement over the previous approach?',answer_vi:'Fiber = rewrite của reconciliation engine (React 16+). Trước: recursive, synchronous (stack) → block main thread. Fiber: incremental rendering — chia work thành units, có thể pause/resume/prioritize. Enables: concurrent mode, Suspense, time-slicing, priority updates.',answer_en:'Fiber = rewrite of reconciliation engine (React 16+). Before: recursive, synchronous (stack) → blocks main thread. Fiber: incremental rendering — splits work into units, can pause/resume/prioritize. Enables: concurrent mode, Suspense, time-slicing, priority updates.',topic:'React',difficulty:'Hard'},
{id:'react-suspense',question:'What is React Suspense and what does it enable?',answer_vi:'Suspense = mechanism "chờ" async operations và hiện fallback UI. <Suspense fallback={<Loading/>}><Component/></Suspense>. Dùng cho: 1) lazy loading (React.lazy) 2) Data fetching (React 18+) 3) Server Components. Component "throw" promise → Suspense catches, hiện fallback.',answer_en:'Suspense = mechanism that "waits" for async operations and shows fallback UI. <Suspense fallback={<Loading/>}><Component/></Suspense>. Used for: 1) Lazy loading (React.lazy) 2) Data fetching (React 18+) 3) Server Components. Component "throws" promise → Suspense catches it, shows fallback.',topic:'React',difficulty:'Medium'},
{id:'react-error-boundaries',question:'What are error boundaries in React for?',answer_vi:'Error Boundary = class component bắt JS errors trong child component tree → hiện fallback UI thay vì crash toàn app. Dùng componentDidCatch + getDerivedStateFromError. KHÔNG bắt: event handlers, async code, SSR, errors trong chính boundary. Bọc sections quan trọng.',answer_en:'Error Boundary = class component that catches JS errors in child component tree → shows fallback UI instead of crashing entire app. Uses componentDidCatch + getDerivedStateFromError. Does NOT catch: event handlers, async code, SSR, errors in boundary itself. Wrap critical sections.',topic:'React',difficulty:'Medium'},
{id:'react-portals',question:'What are React Portals used for?',answer_vi:'Portal render children vào DOM node NGOÀI parent hierarchy: createPortal(children, domNode). Dùng cho: modals, tooltips, dropdowns cần "thoát" khỏi overflow: hidden hoặc z-index của parent. Events vẫn bubble qua React tree (không phải DOM tree).',answer_en:'Portal renders children into a DOM node OUTSIDE parent hierarchy: createPortal(children, domNode). Used for: modals, tooltips, dropdowns that need to "escape" parent\'s overflow: hidden or z-index. Events still bubble through React tree (not DOM tree).',topic:'React',difficulty:'Hard'},
{id:'react-context-pitfalls',question:'What are some pitfalls about using context in React?',answer_vi:'1) ALL consumers re-render khi context value thay đổi — dù chỉ dùng 1 field 2) Không thể skip re-render (React.memo không giúp) 3) Khó debug — data flow ẩn. Fix: split contexts, useMemo value, hoặc dùng state manager cho frequent updates.',answer_en:'1) ALL consumers re-render when context value changes — even if only using 1 field 2) Can\'t skip re-renders (React.memo won\'t help) 3) Hard to debug — hidden data flow. Fix: split contexts, useMemo value, or use state manager for frequent updates.',topic:'React',difficulty:'Medium'},
{id:'react-forwardref',question:'What is `forwardRef()` in React used for?',answer_vi:'forwardRef cho phép component cha truy cập DOM node của component con. Thường component con không expose ref. forwardRef(props, ref) → gắn ref vào inner element. Dùng cho: reusable input components, focus management, measuring DOM. useImperativeHandle tùy chỉnh ref API.',answer_en:'forwardRef allows parent component to access the DOM node of a child component. Normally child doesn\'t expose ref. forwardRef(props, ref) → attach ref to inner element. Used for: reusable input components, focus management, measuring DOM. useImperativeHandle customizes ref API.',topic:'React',difficulty:'Medium'},
{id:'react-code-splitting',question:'What is code splitting in a React application?',answer_vi:'Code splitting = chia bundle thành chunks nhỏ, load theo nhu cầu. React: React.lazy(() => import("./Component")) + Suspense. Next.js: dynamic(). Route-based splitting phổ biến nhất. Giảm initial bundle size → trang load nhanh hơn.',answer_en:'Code splitting = split bundle into smaller chunks, load on demand. React: React.lazy(() => import("./Component")) + Suspense. Next.js: dynamic(). Route-based splitting is most common. Reduces initial bundle size → faster page load.',topic:'React',difficulty:'Medium'},
{id:'react-ssr',question:'Explain server-side rendering of React applications and its benefits',answer_vi:'SSR = render React trên server → gửi HTML hoàn chỉnh về client → hydrate. Lợi ích: 1) SEO (crawler thấy content) 2) Fast FCP (HTML có content ngay) 3) Social media previews. Trade-off: server load, TTFB slower, hydration mismatch bugs. Next.js là framework SSR phổ biến nhất.',answer_en:'SSR = render React on server → send complete HTML to client → hydrate. Benefits: 1) SEO (crawlers see content) 2) Fast FCP (HTML has content immediately) 3) Social media previews. Trade-offs: server load, slower TTFB, hydration mismatch bugs. Next.js is the most popular SSR framework.',topic:'React',difficulty:'Medium'},
{id:'react-hydration',question:'Explain what React hydration is',answer_vi:'Hydration = React "gắn" event listeners và state vào HTML đã SSR. Server gửi static HTML → browser render ngay (fast) → React hydrate: attach event handlers mà KHÔNG tạo lại DOM. Mismatch giữa server/client HTML → hydration error. React 18: Selective Hydration.',answer_en:'Hydration = React "attaches" event listeners and state to SSR\'d HTML. Server sends static HTML → browser renders immediately (fast) → React hydrates: attaches event handlers WITHOUT recreating DOM. Mismatch between server/client HTML → hydration error. React 18: Selective Hydration.',topic:'React',difficulty:'Hard'},
{id:'react-testing',question:'How do you test React applications?',answer_vi:'Unit: React Testing Library + Jest — render component, query DOM, simulate events, assert. Integration: test component interactions. E2E: Playwright/Cypress — simulate real user flows. Testing Library philosophy: test BEHAVIOR không test implementation. Avoid enzyme, test từ user perspective.',answer_en:'Unit: React Testing Library + Jest — render component, query DOM, simulate events, assert. Integration: test component interactions. E2E: Playwright/Cypress — simulate real user flows. Testing Library philosophy: test BEHAVIOR not implementation. Avoid enzyme, test from user perspective.',topic:'React',difficulty:'Medium'},
{id:'react-strict-mode',question:'What is React strict mode and what are its benefits?',answer_vi:'<StrictMode> bật thêm checks trong development: 1) Double-invoke effects/state để tìm impure renders 2) Warn deprecated APIs 3) Detect side effects trong render. CHỈ chạy trong dev, không ảnh hưởng production. React 18 strict mode double-mounts useEffect.',answer_en:'<StrictMode> enables extra checks in development: 1) Double-invoke effects/state to find impure renders 2) Warns deprecated APIs 3) Detects side effects in render. ONLY runs in dev, doesn\'t affect production. React 18 strict mode double-mounts useEffect.',topic:'React',difficulty:'Medium'},
{id:'react-useState-setter',question:'Explain what happens when the `useState` setter function is called in React',answer_vi:'setState(newValue): 1) React lên lịch re-render (KHÔNG chạy ngay) 2) Batch nhiều setState trong cùng event 3) Re-render: gọi lại component function với state mới 4) Diff virtual DOM 5) Update real DOM. setState(prev => prev + 1) khi cần previous state.',answer_en:'setState(newValue): 1) React schedules a re-render (NOT immediate) 2) Batches multiple setStates in same event 3) Re-render: calls component function again with new state 4) Diffs virtual DOM 5) Updates real DOM. setState(prev => prev + 1) when needing previous state.',topic:'React',difficulty:'Medium'},
{id:'react-hoc',question:'What are higher order components in React?',answer_vi:'HOC = function nhận component, return component MỚI với thêm props/logic. Pattern: withAuth(Component) → enhanced component. Dùng cho: auth guards, logging, theming. Nhược: wrapper hell, prop collision, khó trace. Hiện tại custom hooks thay thế HOC hầu hết cases.',answer_en:'HOC = function that takes a component, returns NEW component with added props/logic. Pattern: withAuth(Component) → enhanced component. Used for: auth guards, logging, theming. Downsides: wrapper hell, prop collision, hard to trace. Nowadays custom hooks replace HOC in most cases.',topic:'React',difficulty:'Medium'},
{id:'react-one-way-data',question:'Explain one-way data flow of React and its benefits',answer_vi:'One-way = data chỉ đi TỪ PARENT → CHILD qua props. Child KHÔNG thể sửa props — chỉ gọi callback từ parent. Lợi ích: 1) Dễ debug (biết data từ đâu) 2) Predictable 3) Single source of truth. Two-way (Angular) tự động sync — tiện nhưng khó trace.',answer_en:'One-way = data flows FROM PARENT → CHILD via props. Child CANNOT modify props — only calls parent callbacks. Benefits: 1) Easy debugging (know where data comes from) 2) Predictable 3) Single source of truth. Two-way (Angular) auto-syncs — convenient but harder to trace.',topic:'React',difficulty:'Medium'},
// ═══════════════════════════════════════
//  CSS
// ═══════════════════════════════════════
{id:'css-box-model',question:'Explain your understanding of the box model and how you would tell the browser to render your layout in different box models',answer_vi:'Box model: mỗi element = content + padding + border + margin. 2 loại: content-box (default): width = chỉ content. border-box: width = content + padding + border. box-sizing: border-box dễ tính toán hơn. * { box-sizing: border-box } là best practice.',answer_en:'Box model: every element = content + padding + border + margin. 2 types: content-box (default): width = content only. border-box: width = content + padding + border. box-sizing: border-box is easier to calculate. * { box-sizing: border-box } is best practice.',topic:'CSS',difficulty:'Medium'},
{id:'css-border-box',question:'What does `* { box-sizing: border-box; }` do?',answer_vi:'Áp dụng border-box cho TẤT CẢ elements. width/height bao gồm cả padding + border (không phải chỉ content). Ví dụ: width: 200px + padding: 20px = element vẫn 200px (thay vì 240px). Dễ layout, responsive design đơn giản hơn.',answer_en:'Applies border-box to ALL elements. width/height includes padding + border (not just content). Example: width: 200px + padding: 20px = element stays 200px (instead of 240px). Easier layout, simpler responsive design.',topic:'CSS',difficulty:'Medium'},
{id:'css-display',question:'What is the CSS `display` property and can you give a few examples of its use?',answer_vi:'display xác định loại box: block (chiếm full width, xuống dòng), inline (chiếm đúng content, không set width/height), inline-block (inline nhưng set được width/height), flex (flex container), grid (grid container), none (ẩn + xóa khỏi layout).',answer_en:'display determines box type: block (full width, new line), inline (content-width only, no width/height), inline-block (inline but allows width/height), flex (flex container), grid (grid container), none (hidden + removed from layout).',topic:'CSS',difficulty:'Medium'},
{id:'css-specificity',question:'What is CSS selector specificity and how does it work?',answer_vi:'Specificity = trọng số quyết định CSS rule nào "thắng". Tính: inline (1000) > #id (100) > .class/[attr]/:pseudo (10) > tag/::pseudo (1) > * (0). !important thắng tất cả. Cùng specificity → rule cuối thắng. Tránh !important, dùng class-based selectors.',answer_en:'Specificity = weight deciding which CSS rule "wins". Calculation: inline (1000) > #id (100) > .class/[attr]/:pseudo (10) > tag/::pseudo (1) > * (0). !important overrides all. Same specificity → last rule wins. Avoid !important, use class-based selectors.',topic:'CSS',difficulty:'Hard'},
{id:'css-block-inline',question:"What's the difference between `block`, `inline`, and `inline-block`?",answer_vi:'block: chiếm full width, xuống dòng, set được width/height/margin (div, p, h1). inline: chiếm đúng content, KHÔNG xuống dòng, KHÔNG set width/height (span, a, strong). inline-block: inline nhưng set ĐƯỢC width/height/margin (img, button).',answer_en:'block: takes full width, new line, can set width/height/margin (div, p, h1). inline: wraps content only, NO new line, CANNOT set width/height (span, a, strong). inline-block: inline but CAN set width/height/margin (img, button).',topic:'CSS',difficulty:'Easy'},
{id:'css-position',question:"What's the difference between `relative`, `fixed`, `absolute`, `sticky` and `static`?",answer_vi:'static: default, normal flow. relative: normal flow + offset bằng top/left (tham chiếu vị trí gốc). absolute: thoát flow, relative to nearest positioned ancestor. fixed: thoát flow, relative to viewport (scroll không ảnh hưởng). sticky: like relative → becomes fixed khi scroll đến threshold.',answer_en:'static: default, normal flow. relative: normal flow + offset with top/left (relative to original position). absolute: leaves flow, relative to nearest positioned ancestor. fixed: leaves flow, relative to viewport (unaffected by scroll). sticky: like relative → becomes fixed when scrolling past threshold.',topic:'CSS',difficulty:'Medium'},
{id:'css-flexbox-grid',question:'Have you played around with the new CSS Flexbox or Grid specs?',answer_vi:'Flexbox: 1D layout (row HOẶC column). justify-content (main axis), align-items (cross axis). Grid: 2D layout (rows VÀ columns cùng lúc). grid-template-columns/rows, grid-area. Flexbox cho component layout, Grid cho page layout. Cả hai hỗ trợ gap property.',answer_en:'Flexbox: 1D layout (row OR column). justify-content (main axis), align-items (cross axis). Grid: 2D layout (rows AND columns simultaneously). grid-template-columns/rows, grid-area. Flexbox for component layouts, Grid for page layouts. Both support gap property.',topic:'CSS',difficulty:'Medium'},
{id:'css-z-index',question:'Describe `z-index` and how stacking context is formed',answer_vi:'z-index kiểm soát thứ tự chồng trên trục Z. CHỈ hoạt động với positioned elements (relative/absolute/fixed/sticky). Stacking context mới tạo bởi: position + z-index, opacity < 1, transform, filter. z-index chỉ so sánh TRONG CÙNG stacking context.',answer_en:'z-index controls stacking order on Z-axis. ONLY works with positioned elements (relative/absolute/fixed/sticky). New stacking context created by: position + z-index, opacity < 1, transform, filter. z-index only compared WITHIN the same stacking context.',topic:'CSS',difficulty:'Hard'},
{id:'css-bfc',question:'Describe Block Formatting Context (BFC) and how it works',answer_vi:'BFC = vùng layout độc lập — elements bên trong không ảnh hưởng bên ngoài. Tạo BFC: overflow != visible, display: flow-root, float, position: absolute/fixed, flex/grid. BFC: chứa floats, ngăn margin collapse, không overlap floated siblings.',answer_en:'BFC = independent layout region — elements inside don\'t affect outside. Create BFC: overflow != visible, display: flow-root, float, position: absolute/fixed, flex/grid. BFC: contains floats, prevents margin collapse, doesn\'t overlap floated siblings.',topic:'CSS',difficulty:'Hard'},
{id:'css-pseudo-elements',question:'Describe pseudo-elements and discuss what they are used for',answer_vi:'Pseudo-elements (::before, ::after, ::first-line, ::first-letter) tạo/style phần tử ẢO không có trong HTML. ::before/::after cần content property. Dùng cho: decorative elements, icons, clearfix, tooltips, custom bullets. Pseudo-class (:hover, :focus) style STATE.',answer_en:'Pseudo-elements (::before, ::after, ::first-line, ::first-letter) create/style VIRTUAL elements not in HTML. ::before/::after need content property. Used for: decorative elements, icons, clearfix, tooltips, custom bullets. Pseudo-class (:hover, :focus) styles STATE.',topic:'CSS',difficulty:'Medium'},
{id:'css-responsive-mobile-first',question:'Can you explain the difference between coding a website to be responsive versus using a mobile-first strategy?',answer_vi:'Responsive: design cho desktop trước, dùng media queries thu nhỏ xuống mobile (max-width). Mobile-first: design cho mobile trước, dùng media queries mở rộng lên desktop (min-width). Mobile-first tốt hơn: progressive enhancement, fast trên mobile, ít override CSS.',answer_en:'Responsive: design for desktop first, use media queries to scale down to mobile (max-width). Mobile-first: design for mobile first, use media queries to scale up to desktop (min-width). Mobile-first is better: progressive enhancement, fast on mobile, fewer CSS overrides.',topic:'CSS',difficulty:'Medium'},
{id:'css-translate-vs-position',question:'Is there any reason you\'d want to use `translate()` instead of `absolute` positioning?',answer_vi:'translate() dùng GPU (compositor layer) → smoother animations, không trigger layout/repaint. position: absolute trigger layout recalculation. translate cho animations/transitions. absolute cho static positioning. transform: translate KHÔNG ảnh hưởng document flow.',answer_en:'translate() uses GPU (compositor layer) → smoother animations, no layout/repaint trigger. position: absolute triggers layout recalculation. translate for animations/transitions. absolute for static positioning. transform: translate does NOT affect document flow.',topic:'CSS',difficulty:'Hard'},
// ═══════════════════════════════════════
//  HTML
// ═══════════════════════════════════════
{id:'html-doctype',question:'What does a `DOCTYPE` do?',answer_vi:'<!DOCTYPE html> nói browser dùng standards mode (HTML5). Không có → quirks mode (IE5 behavior, box model khác). Phải đặt DÒNG ĐẦU TIÊN của file HTML. HTML5 DOCTYPE đơn giản nhất: <!DOCTYPE html>. Không phải HTML tag — là instruction cho browser.',answer_en:'<!DOCTYPE html> tells browser to use standards mode (HTML5). Without it → quirks mode (IE5 behavior, different box model). Must be FIRST LINE of HTML file. HTML5 DOCTYPE is simplest: <!DOCTYPE html>. Not an HTML tag — it\'s an instruction for the browser.',topic:'HTML',difficulty:'Hard'},
{id:'html-data-attributes',question:'What are `data-` attributes good for?',answer_vi:'data-* attributes lưu CUSTOM data trên HTML elements: <div data-user-id="123">. Truy cập: element.dataset.userId. Dùng cho: pass data từ HTML → JS, testing selectors ([data-testid="button"]), styling ([data-active]). Không ảnh hưởng layout/style mặc định.',answer_en:'data-* attributes store CUSTOM data on HTML elements: <div data-user-id="123">. Access: element.dataset.userId. Used for: passing data from HTML → JS, testing selectors ([data-testid="button"]), styling ([data-active]). Doesn\'t affect default layout/style.',topic:'HTML',difficulty:'Medium'},
{id:'html-semantic',question:'Consider HTML5 as an open web platform. What are the building blocks of HTML5?',answer_vi:'1) Semantics: <header>, <nav>, <main>, <article>, <section>, <footer> 2) Multimedia: <video>, <audio>, <canvas> 3) Storage: localStorage, sessionStorage 4) Connectivity: WebSocket 5) Graphics: Canvas, SVG 6) Performance: Web Workers 7) Device access: Geolocation.',answer_en:'1) Semantics: <header>, <nav>, <main>, <article>, <section>, <footer> 2) Multimedia: <video>, <audio>, <canvas> 3) Storage: localStorage, sessionStorage 4) Connectivity: WebSocket 5) Graphics: Canvas, SVG 6) Performance: Web Workers 7) Device access: Geolocation.',topic:'HTML',difficulty:'Medium'},
{id:'html-link-script-placement',question:'Why is it generally a good idea to position CSS `<link>`s in `<head>` and JS `<script>`s before `</body>`?',answer_vi:'CSS trong <head>: browser xây render tree sớm → tránh FOUC (flash of unstyled content). JS trước </body>: không block HTML parse → trang hiện nhanh hơn. Hoặc dùng defer/async. CSS block rendering, JS block parsing.',answer_en:'CSS in <head>: browser builds render tree early → avoids FOUC (flash of unstyled content). JS before </body>: doesn\'t block HTML parsing → page appears faster. Or use defer/async. CSS blocks rendering, JS blocks parsing.',topic:'HTML',difficulty:'Medium'},
{id:'html-progressive-rendering',question:'What is progressive rendering?',answer_vi:'Progressive rendering = hiển thị content SỚM NHẤT có thể, không đợi tải hết. Kỹ thuật: 1) Lazy loading images 2) Server-side rendering 3) Async CSS/JS 4) Skeleton screens 5) Code splitting 6) Priority hints. Cải thiện perceived performance.',answer_en:'Progressive rendering = display content AS SOON AS possible, don\'t wait for everything to load. Techniques: 1) Lazy loading images 2) Server-side rendering 3) Async CSS/JS 4) Skeleton screens 5) Code splitting 6) Priority hints. Improves perceived performance.',topic:'HTML',difficulty:'Medium'},
{id:'html-spa-seo',question:'Explain what a single page app is and how to make one SEO-friendly',answer_vi:'SPA = 1 HTML page, JS handle routing + render (React, Vue). SEO problems: crawler thấy empty HTML. Solutions: 1) SSR (Next.js) 2) Static generation (SSG) 3) Dynamic rendering 4) Pre-rendering. Meta tags + structured data + sitemap.xml. Core Web Vitals matter.',answer_en:'SPA = 1 HTML page, JS handles routing + rendering (React, Vue). SEO problems: crawler sees empty HTML. Solutions: 1) SSR (Next.js) 2) Static generation (SSG) 3) Dynamic rendering 4) Pre-rendering. Meta tags + structured data + sitemap.xml. Core Web Vitals matter.',topic:'HTML',difficulty:'Hard'},
]
